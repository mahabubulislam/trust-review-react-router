import React from 'react';

const Review = ({ review }) => {
    const { userName, userImg, reviewText, rating } = review
    return (
        <>
            
        </>
    );
};

export default Review;